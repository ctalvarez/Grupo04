<h1 class="edit-h1">Add New Season</h1>

<%= render 'form', session: @season, serie: @season.serie, count_s: @season.serie.sessions.count %>

<%= link_to 'Go Back', '/series/' + @serie.id.to_s, :class=>"btn btn-primary edit-back"  %>
